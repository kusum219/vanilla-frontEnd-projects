//for toogling nav-bar in mobile
const navLinks = document.getElementById('nav-links');

function showMenu() {
    navLinks.style.display = "block"; // Show the menu
}

function hideMenu() {
    navLinks.style.display = "none"; // Hide the menu
}

